```{=html}
<ul class="list list-group list-group-flush">
<% for (const item of items) { %>
  <li <%= metadataAttrs(item) %> class="list-group-item d-sm-flex flex-sm-row-reverse justify-content-sm-between">
    <div>
      <p class="listing-date text-nowrap text-end mb-0 mb-md-3"><%- item.date %></p>
    </div>
    <div>
      <a class="listing-link" href="<%- item.path %>">
        <iconify-icon icon="fluent-emoji:link"></iconify-icon>
        <%= item.title %><span> </span>
      </a>
    </div>
  </li>
<% } %>
</ul>
```
